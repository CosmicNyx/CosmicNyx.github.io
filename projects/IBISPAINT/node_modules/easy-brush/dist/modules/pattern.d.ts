import { PatternBasicConfig, PatternConfig } from "../types/pattern";
/**
 * @param {HTMLCanvasElement} canvas original canvas. Used to obtain width and height
 */
export declare class PatternModule {
    /** module config */
    config: PatternBasicConfig;
    private pattern;
    private patternCanvas;
    private patternContext;
    private patternBlendCanvas;
    private patternBlendContext;
    constructor(config?: PatternConfig);
    private loadImageWithUrl;
    private loadImageWithElement;
    /**
     * Load pattern image resource
     *
     * If there is no width height, the image will repeat
     * @param resource pattern image resource
     * @param width canvas width
     * @param height canvas height
     */
    loadPattern(resource: HTMLImageElement | HTMLCanvasElement | string, canvasWidth: number, canvasHeight: number, patternColor?: string): Promise<void>;
    removePattern(): void;
    /**
     * Bind config to brush.
     *
     * If you do this, the brush config will change with the external config
     */
    bindConfig(config: PatternBasicConfig): void;
    onMixinCanvas(strokeCanvas: HTMLCanvasElement, strokeContext: CanvasRenderingContext2D): [HTMLCanvasElement, CanvasRenderingContext2D];
}
