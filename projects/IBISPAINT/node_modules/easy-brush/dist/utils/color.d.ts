/**
 * 将输入的字符串颜色转换为标准哈希颜色格式。
 * @param color 输入的颜色字符串，可以是rgb、rgba、三位哈希(#fff)或六位哈希(#ffffff)等格式。
 * @returns 标准的六位哈希颜色字符串，例如：#000000。如果输入格式不合法，则返回默认颜色#000000。
 */
export declare const toHashColor: (color: string) => string;
